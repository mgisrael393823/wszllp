import React from 'react';

/**
 * Placeholder NotificationScheduler component
 * The notification features have been deferred for the MVP
 */
const NotificationScheduler: React.FC = () => {
  // This is a placeholder component that doesn't render anything
  return null;
};

export default NotificationScheduler;