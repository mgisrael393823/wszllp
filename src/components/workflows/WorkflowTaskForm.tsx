import React from 'react';
import { Card } from '../ui/shadcn-card';

/**
 * Placeholder WorkflowTaskForm component
 * The workflow automation features have been deferred for the MVP
 */
const WorkflowTaskForm: React.FC = () => {
  return (
    <Card className="p-6">
      <h2 className="text-xl font-semibold mb-4">Workflow Task Form</h2>
      <p className="text-neutral-500">
        Workflow task form will be available in a future update.
      </p>
    </Card>
  );
};

export default WorkflowTaskForm;