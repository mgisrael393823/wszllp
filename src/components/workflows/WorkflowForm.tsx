import React from 'react';
import Card from '../ui/Card';

/**
 * Placeholder WorkflowForm component
 * The workflow automation features have been deferred for the MVP
 */
const WorkflowForm: React.FC = () => {
  return (
    <Card className="p-6">
      <h2 className="text-xl font-semibold mb-4">Workflow Form</h2>
      <p className="text-neutral-500">
        Workflow form will be available in a future update.
      </p>
    </Card>
  );
};

export default WorkflowForm;