import React from 'react';
import Card from '../ui/Card';

/**
 * Placeholder CalendarMonthView component
 * The calendar features have been deferred for the MVP
 */
const CalendarMonthView: React.FC = () => {
  return (
    <Card className="p-6">
      <h2 className="text-xl font-semibold mb-4">Calendar Month View</h2>
      <p className="text-gray-500">
        Calendar month view will be available in a future update.
      </p>
    </Card>
  );
};

export default CalendarMonthView;