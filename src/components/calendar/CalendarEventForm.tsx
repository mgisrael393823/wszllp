import React from 'react';
import { Card } from '../ui/shadcn-card';

/**
 * Placeholder CalendarEventForm component
 * The calendar features have been deferred for the MVP
 */
const CalendarEventForm: React.FC = () => {
  return (
    <Card className="p-6">
      <h2 className="text-xl font-semibold mb-4">Calendar Event Form</h2>
      <p className="text-neutral-500">
        Calendar event form will be available in a future update.
      </p>
    </Card>
  );
};

export default CalendarEventForm;